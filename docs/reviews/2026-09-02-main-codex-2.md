---
artifact_id: "2026-09-02-main-codex-2"
date: "2026-09-02"
producer: "codex"
run_kind: "codex-adversarial"
verdict: "PASS-WITH-FINDINGS"
branch: "main"
base_ref: "origin/main"
base_sha: "7eb7be1"
head_sha: "f89dabd"
base_ref_source: "nearest-of-2"
worktree_dirty: true
redactions: 0
pr: null
session_id: null
agent_transcript_path: null
repo_root_source: "explicit_repo_arg"
files_reviewed:
  - "tests/test_method_consistency.py"
findings:
  - id: "F1"
    severity: "UNSPECIFIED"
    file: "tests/test_method_consistency.py"
    line: 818
    claim: "MED — NodeReal's wire predicate verifies only `fromBlock=0x64`; it passes even if `toBlock=0xc8` is omitted. Fix: assert the exact RPC filter contains both `fromBlock` and `toBlock`"
    disposition: "accepted"
    disposition_note: "fixed in f89dabd: predicate asserts fromBlock=0x64 AND toBlock=0xc8"
---

# codex review — main

> Captured automatically by `tools/hooks/review_capture.py` (agent-skills). The report below is the reviewer's verbatim output; the frontmatter above is parsed from it. Set each finding's `disposition` when you action it.

## Report (verbatim)

Verdict: PASS-WITH-FINDINGS

1. MED — [tests/test_method_consistency.py:818] NodeReal's wire predicate verifies only `fromBlock=0x64`; it passes even if `toBlock=0xc8` is omitted. Fix: assert the exact RPC filter contains both `fromBlock` and `toBlock`.

Verified:

- Direct `get_transactions(..., start_block=100, end_block=200)` on real `BlockScoutV2Scanner` raises before any fake-network request.
- `fetch_page()` has the same zero-wire-call guard.
- Unbounded direct calls still reach the wire.
- All targeted tests pass: `104 passed`.
- `QUICK_REFERENCE.md` has no remaining `aiochainscan.core.method` imports.
- No new production defect found in `051f6bb`.

Independent external review could not run: Cursor failed twice before review with sandbox-denied writes to `~/.cursor/cli-config.json`; no repository files were modified.
