---
artifact_id: "2026-09-02-main-codex-3"
date: "2026-09-02"
producer: "codex"
run_kind: "codex-adversarial"
verdict: "PASS"
branch: "main"
base_ref: "origin/main"
base_sha: "7eb7be1"
head_sha: "7d9269e"
base_ref_source: "nearest-of-2"
worktree_dirty: false
redactions: 0
pr: null
session_id: null
agent_transcript_path: null
repo_root_source: "explicit_repo_arg"
files_reviewed:
  - "/Users/andrew/Documents/projects/aiochainscan/aiochainscan/core/pool.py"
  - "/Users/andrew/Documents/projects/aiochainscan/aiochainscan/network.py"
findings: []
---

# codex review — main

> Captured automatically by `tools/hooks/review_capture.py` (agent-skills). The report below is the reviewer's verbatim output; the frontmatter above is parsed from it. Set each finding's `disposition` when you action it.

## Report (verbatim)

## Review

Scope: `87c9778fe0a6700646363116aa01ee80cfcb4674` on `main`
Verdict: **PASS**

No confirmed code findings.

- `401/403` are classified at the sole HTTP status raise seam, while `400/402/404` remain `FATAL`: [network.py](/Users/andrew/Documents/projects/aiochainscan/aiochainscan/network.py:619).
- `ChainscanPool` receives the carried `AUTH` kind, fails over, and applies its 600-second cooldown: [pool.py](/Users/andrew/Documents/projects/aiochainscan/aiochainscan/core/pool.py:510), [pool.py](/Users/andrew/Documents/projects/aiochainscan/aiochainscan/core/pool.py:445).
- Single-provider behavior retains the same exception type/message and retry behavior; only the intentional observable `failure_kind` changes.
- No stale 401/403 tests found. The new parametrized test asserts both mappings and preserves `400 → FATAL`; the pool test confirms wire-originated `401` failover.
- No missed 401/403-to-FATAL HTTP paths found: first-request chain guard, chain-info probes, BlockScout RPC, and NodeReal RPC/REST all use `Network.request`; scanner translation re-raises `ChainscanClientError` unchanged. A focused mocked first-guard `403` check retained `FailureKind.AUTH`.
- The repository provides no concrete provider-specific HTTP-403 plan restriction path. Etherscan plan errors remain envelope/text-classified; treating generic 403 as `AUTH` correctly preserves failover for the supported paths.

Verification: focused tests passed — `6 passed`:
`test_http_status_raise_sites_carry_failure_kinds` and the wire-to-pool failover test.

Gap: the required external Cursor reviewer could not start after its one retry because sandbox permissions prevented writing its CLI config under `~/.cursor`; both attempts failed with `EPERM`. No repository files were modified.
